# wire:model

{{ include("admonition/livewire-reference.md", reference_url="https://livewire.laravel.com/docs/wire-model") }}

!!! info "Migrating from Magewire V1"
    V1's `wire:model` synced live by default. V3 flips the default — `wire:model` defers, and `.live` opts in. `.lazy` is now `.blur`. See [Upgrade](../getting-started/upgrade.md).
